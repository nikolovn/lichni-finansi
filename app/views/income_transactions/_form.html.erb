<%= form_for(@income_transaction) do |f| %>

  <% if @income_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@income_transaction.errors.count, "error") %> prohibited this income_transaction from being saved:</h2>
      
      <ul>
      <% @income_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


 <div class="form-group">
      <%= f.hidden_field :income_category_id, :value => income_category_id, :id => 'income_category_id'  %>
      <%= f.label :description %><br>
      <%= f.text_area :description, class: "form-control" %><br>
      <%= f.label :amount %><br>
      <%= f.text_field :amount, class: "form-control" %>
      <%= f.label :date %><br>
      <%= f.text_field :date, class: "form-control" %>
  </div>
 <%= f.submit %>

 

    
 
  
<% end %>
