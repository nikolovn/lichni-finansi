<%= form_for(@expense_transaction) do |f| %>
  <% if @expense_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense_transaction.errors.count, "error") %> prohibited this expense_transaction from being saved:</h2>
      
      <ul>
      <% @expense_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div id = "income_transaction">

     <%= f.hidden_field :expense_category_id, :value => expense_category_id, :id => 'expense_category_id'  %>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description %><br>
      <%= f.label :date %><br>
      <%= f.text_field :date %><br>
      <%= f.label :amount %><br>
      <%= f.text_field :amount %><br>
      <%= f.label :income_relation %><br>
      <%= f.collection_select :income_relation, @income_transactions, :id, :description, include_blank: true %><br>
      <%= f.label :TYPE %><br>
      <%= f.select :expense_type, options_for_select([['investment' ,'investment'], ['saving', 'saving'], ['expense', 'expense']]) %>



    </div>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>
<% end %>
