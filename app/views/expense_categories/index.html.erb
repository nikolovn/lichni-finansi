
  <div class="panel panel-default">
    <div class="panel-heading">Income transactions</div>
    <div class="panel-body">
<table class="table table-bordered table-hover table-condensed table-striped table-responsive">        <thead>
          <tr>
            <th>Category</th>
            <th>Action</th>
          </tr>
        </thead>
 
 
    <% @expense_parent_categories.each do |category| %>
          
            <tr>
              <td>
                  <%= link_to "#{'-' * category.level} #{category.name}",show_sub_category_expense_categories_path(category.id), remote: true %>
              </td>
              <td>
                <%= link_to 'Edit', edit_expense_category_path(category) %>
                <%= link_to 'Delete', expense_category_path(category.id), method: :delete, data: { confirm: 'Are you sure?' } %>
                </p>
                </td>
            </tr>
            <tr>
              <td colspan = 2>
              <div id = "<%=category.name %>" > </div>
              </td>
            </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <%= link_to 'New category', new_expense_category_path %>



    </div>
  </div>